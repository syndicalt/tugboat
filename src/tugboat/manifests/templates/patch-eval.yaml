name: patch-eval
purpose: Evaluate a candidate instruction patch against held-out checks.
inputs:
  - candidate_patch
  - eval_suite
  - policy
outputs:
  - eval_report
  - policy_decision
output_artifacts:
  eval_report: eval-report.raw.json
  policy_decision: policy-decision.raw.json
