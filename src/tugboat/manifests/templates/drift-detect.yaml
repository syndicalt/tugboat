name: drift-detect
purpose: Detect recurring gaps between agent behavior and repository instructions.
inputs:
  - audit_reports
  - instruction_index
  - instruction_index_artifact
  - policy
outputs:
  - drift_clusters
  - optimizer_notes
